import { SET_CURRENT_CLUSTER } from "../Constants";

export function setCurrentCluster(cluster) {
    return {
        type: SET_CURRENT_CLUSTER,
        cluster
    }
}

export function getUserCluster(user) {
        
}